{
  "name": "monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "concurrently \"yarn workspace backend start\" \"yarn workspace frontend start\"",
    "install": "yarn workspace backend run install && yarn workspace frontend run install",
    "build": "yarn workspace frontend run build"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^6.0.2",
    "dotenv": "^10.0.0"
  }
}
